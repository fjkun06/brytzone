import React from "react";
import { brytzone } from "../header";

const HeaderSkeleton = () => {
  return <header className={`${brytzone}_skeleton_home-header`}></header>;
};

export default HeaderSkeleton;
